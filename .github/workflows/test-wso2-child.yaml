name: Trigger Master Workflow

on:
  workflow_dispatch:

jobs:
  call-master:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger child workflow
        uses: kariyasinghe-cell/wso2-workflows/.github/workflows/test-wso2-apis.yaml@main
        with:
          includeFolders: "SampleAPI, v2"
