name: Parent Workflow

on:
  workflow_dispatch:

jobs:
  call-child:
    # Job-level 'uses' references the child workflow
    uses: kariyasinghe-cell/wso2-workflows/.github/workflows/test-wso2-apis.yaml@main
    with:
      includeFolders: ""
      fromChild: "true"
    secrets: inherit   # optional, if child workflow needs repo secrets
